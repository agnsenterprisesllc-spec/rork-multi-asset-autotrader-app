{
  "build": {
    "commands": [
      "pip install -r backend/requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "sh -c 'cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT'"
  }
}
